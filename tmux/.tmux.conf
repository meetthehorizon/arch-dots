# ==============================
# TMUX CONFIG (stow-ready)
# ==============================

# --- Prefix ---
set -g prefix C-a             # Use Ctrl-a instead of Ctrl-b
unbind C-b
bind C-a send-prefix

# --- Vim-style pane navigation ---
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# --- Resize panes with Shift + hjkl ---
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# --- Vi-style copy mode ---
setw -g mode-keys vi

# --- Status bar ---
set -g status-bg colour235
set -g status-fg colour136
set -g status-left "#[fg=green]#S"
set -g status-right "#[fg=yellow]%Y-%m-%d #[fg=cyan]%H:%M"

# --- TPM Plugins ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# --- tmux-continuum auto-restore and auto-save ---
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'  # every 15 minutes

# --- Initialize TMUX plugin manager (keep at bottom) ---
run '~/.tmux/plugins/tpm/tpm'

